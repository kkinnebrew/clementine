/**
 * request-ds.js | OrangeUI Example App 0.1 | 12.21.2011 
 */

if (typeof Orange === 'undefined') {
    var Orange = {};
}

if (typeof Orange.DB === 'undefined') {
    Orange.DB = {};
}

if (typeof Orange.DB.Request === 'undefined') {
    Orange.DB.Request = {};
}

Orange.DB.Request.DataSource = Class.create({

	initialize: function() {
	
	}
	
});