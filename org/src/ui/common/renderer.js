/**
 * renderer.js | OrangeUI Framework 0.1 | 12.21.2011 
 */

if (typeof O === 'undefined') {
    var O = {};
}

if (typeof O.UI === 'undefined') {
    var O.UI = {};
}

O.UI.Renderer = Class.create({

	initialize: function() {
	
	
	}

});