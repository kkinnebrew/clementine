/**
 * processor.js | OrangeUI Framework 0.1 | 12.21.2011 
 */

if (typeof O === 'undefined') {
    var O = {};
}

if (typeof O.UI === 'undefined') {
    var O.UI = {};
}

O.UI.Processor = Class.create({

	initialize: function() {
	
	
	}

});